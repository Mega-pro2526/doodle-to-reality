
## 📁 `docs/api_reference.md`

```md
# 🧪 API Reference

### POST /predict
- Input: `{ image: <base64>, style: <style-name> }`
- Output: `{ prediction: "cat", style: "van-gogh" }`

### POST /story
- Input: `{ label: "cat" }`
- Output: `{ story: "Once upon a time, a clever cat..." }`
